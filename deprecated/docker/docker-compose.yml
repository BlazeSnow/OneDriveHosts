networks:
  blazesnow:
    external: true
    name: blazesnow

services:
  onedrivehosts:
    build:
      context: .
      dockerfile: Dockerfile
    image: registry.cn-hangzhou.aliyuncs.com/blazesnow/onedrivehosts:latest
    container_name: onedrivehosts
    restart: always
    networks:
      - blazesnow
